#!/usr/bin/python3

"""Returns an instance of a class"""

def is_same_class(obj, a_class):
    """ Defined function """
    if type(obj) == a_class:
        return (true)
    else:
        return (false)
